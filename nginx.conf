events {
    worker_connections 1024;
}

http {
    upstream geoserver {
        # GeoServer is running on port 8080 inside the Docker container
        server geoserver:8080;
    }

    # Handle HTTP traffic
    server {
        # Listen on port 80
        listen 80;
        server_name cruisedb.corp.spc.int;

        # Proxy requests to the GeoServer
        location /geoserver/ {
            proxy_pass http://geoserver/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Add more location blocks here for other services or static files if needed
    }

    # Disable the HTTPS configuration for now, as port 443 is not open
    # server {
    #     listen 443 ssl;
    #     server_name cruisedb.corp.spc.int;

    #     # SSL certificate and key files
    #     ssl_certificate /etc/nginx/ssl/selfsigned.crt;
    #     ssl_certificate_key /etc/nginx/ssl/selfsigned.key;

    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_ciphers HIGH:!aNULL:!MD5;

    #     location /geoserver/ {
    #         proxy_pass http://geoserver/;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    # }
}
